Playground for personal AI learning. Very simple agent that connects to Google's 'gemini-2.5-flash' model.

Model's working directory is locked to the calculator package.

API key not included in repository. Must be configured in local .env file.

```
uv run main.py "<prompt>"
```

```
uv run main.py "<prompt>" --verbose
```

